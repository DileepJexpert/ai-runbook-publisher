# Spring rules
Check relevant invocation boundaries, self-invocation, `@Transactional` scope/rollback, remote calls inside transactions, validation boundaries, exception semantics, async context propagation, and mutable singleton state.
