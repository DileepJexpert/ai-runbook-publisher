For every candidate, perform a disproof pass. Inspect direct/indirect callers, callees, helpers, transactions, JPA lifecycle, locks, constraints, configuration and tests. State which files were inspected and why their behavior confirms, downgrades, questions, or rejects the candidate. BLOCKER/MAJOR requires CONFIRMED verification, a populated execution path, PR attribution, counter-evidence checked, and a conclusion explaining why protections do not invalidate the failure. A REJECTED candidate must never appear in final findings. A DOWNGRADED candidate must use a lower final severity.
